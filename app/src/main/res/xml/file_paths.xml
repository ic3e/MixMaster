<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <external-files-path name="reports" path="reports/" />
    <!-- Safety and technical sheets copied into the app, handed to whatever opens a PDF. -->
    <files-path name="sheets" path="sheets/" />
    <!-- The downloaded APK, handed to Android's installer. -->
    <cache-path name="updates" path="updates/" />
    <!-- The company server's files, sent from the setup guide to whoever sets it up. -->
    <cache-path name="server" path="server/" />
</paths>
